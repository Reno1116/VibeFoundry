# 系统协作约定

- 先判断是否需要 brief，再决定是否进入 spec 或 build
- `brief` 不完整时，不进入 `spec`
- `spec` 未写清输入输出、边界、验证方式时，不进入 `build`
- 没有需求卡片的任务，默认不直接开始实现
- 进入 `build` 前先提醒用户做人工确认
- 实现结束后补最小 `validate`
- 读取中文文档时优先按 UTF-8 解码
- 优先输出结构化内容，不跳步骤
- 信息不完整时，先补最小必要澄清
